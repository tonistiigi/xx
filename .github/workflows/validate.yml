name: validate

on:
  push:
    branches:
      - 'master'
      - 'v2'
  pull_request:
    branches:
      - 'master'
      - 'v2'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Validate
        uses: docker/bake-action@v1
        with:
          targets: validate
